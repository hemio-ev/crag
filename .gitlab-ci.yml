build_job:
    script:
        - cabal update
        - cabal sandbox init
        - cabal install --dependencies-only --disable-optimization
        - make pebble
        - cabal install --enable-tests --enable-coverage --disable-optimization
        - cabal test
        - tail -n 2 dist/hpc/vanilla/html/crag-integration/hpc_index.html

